#ifndef VISITOR_HPP
#define VISITOR_HPP

class Visitor
{
public:
    Visitor();
    ~Visitor();
    Visitor(const Visitor& other);
    Visitor& operator=(const Visitor& rhs);

protected:

private:

};

#endif // VISITOR_HPP
